package com.classloader;

import java.lang.reflect.Method;

public class Main {
    public static void main(String[] args) throws Exception {
        // Provide path where Hello.class is located
       /* String classPath = "C:/Users/hemkumar11/eclipse-workspace/POC/target/classes/com/classloader";
        MyClassLoader loader = new MyClassLoader(classPath);

        // Load Hello class dynamically
        Class<?> helloClass = loader.findClass("Hello");
        Object helloObj = helloClass.getDeclaredConstructor().newInstance();

        // Invoke sayHello() using reflection
        Method method = helloClass.getMethod("sayHello");
        method.invoke(helloObj);*/
    }
}
